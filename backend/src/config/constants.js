// Application constants

const API_VERSION = 'v1';
const APP_NAME = 'CRM JURIDICO';
const SUPPORT_EMAIL = 'support@crmjuridico.com';
const DEFAULT_LANGUAGE = 'en';
const TIMEZONE = 'UTC';

module.exports = {
    API_VERSION,
    APP_NAME,
    SUPPORT_EMAIL,
    DEFAULT_LANGUAGE,
    TIMEZONE
};
